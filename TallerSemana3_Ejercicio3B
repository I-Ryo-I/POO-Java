// Java
public HashMap<String, Double> aprobados() {
    HashMap<String, Double> resultado = new HashMap<>();
    for (Map.Entry<String, Double> e : notas.entrySet()) {
        if (e.getValue() >= 3.0) {
            resultado.put(e.getKey(), e.getValue());
        }
    }
    return resultado;
}
